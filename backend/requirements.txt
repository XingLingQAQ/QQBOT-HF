# Backend (manager) runtime dependencies.
# These run in the system Python interpreter (supervisor) and inside /data/venv
# for the FastAPI backend. NoneBot + adapter are installed into /data/venv by
# docker-entrypoint.sh on first boot (kept out of this file to avoid duplicate
# image layers), but listed in a comment below for reference.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
itsdangerous>=2.1
aiofiles>=23.2
websockets>=12.0

# Installed into /data/venv by the entrypoint on first run (not part of the image):
#   nonebot2>=2.5.0
#   nonebot-adapter-onebot>=2.4.6
