# VincentZyu233/SignServer runtime config.
# offset/version are reverse-engineered for the exact Linux QQ build whose
# wrapper.node is shipped alongside the `sign` binary. Do NOT change `version`
# or `offset` unless you also swap wrapper.node for the matching QQ build.
preloads = ["libgnutls.so.30", "./libsymbols.so"]
listen = "127.0.0.1:8087"
offset = 0x5ADE220
version = "3.2.19-39038"
